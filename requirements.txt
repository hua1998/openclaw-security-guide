# OpenClaw Security Tools - Python 依赖
# 安装命令: pip install -r requirements.txt

# 核心依赖
pyyaml>=6.0
cryptography>=41.0.0
watchdog>=3.0.0
aiohttp>=3.8.0

# 测试依赖
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# 代码质量工具
flake8>=6.0.0
pylint>=2.17.0
black>=23.0.0

# 类型检查 (可选)
mypy>=1.5.0

# 注意: 如果在 macOS 上遇到权限问题，可以使用:
#   pip install --user -r requirements.txt
# 或者使用虚拟环境:
#   python3 -m venv venv
#   source venv/bin/activate  # Linux/Mac
#   venv\Scripts\activate     # Windows
#   pip install -r requirements.txt
